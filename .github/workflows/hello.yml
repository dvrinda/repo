name: My First Action

on: [workflow_dispatch]  # Lets you trigger manually

jobs:
  test:
    runs-on: ubuntu-latest  # Free Linux machine provided by GitHub
    steps:
      - name: 21f1001205@ds.study.iitm.ac.in  # Your required step name
        run: echo "Hello from my GitHub Action!"
